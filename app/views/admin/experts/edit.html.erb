<div id="notice">
  <h1>编辑专家</h1>
</div>
<div id="side">
  <%= render "admin/shared/shortcut" %>
</div>
<div id="main">
  <div class="portlet">
    <div class="title"></div>
    <div class="content">
      <%= form_for [:admin, @expert] do |f| %>
      <%= render "admin/shared/error_messages", :target => @expert %>
      <fieldset class="form-generic">
        <legend>专家信息</legend>
        <ul>
          <li>
            <label>账号：</label><%= @expert.account %>
          </li>
          <li>
            <label>姓名：</label><%= f.text_field :name, :class => 'text-field' %>
          </li>
        </ul>
      </fieldset>
      <div class="action-generic">
        <%= link_to '修改', '#', :onclick => "submitForm('edit_expert_#{@expert.id}')", :class => 'button' %>
      </div>
      <% end %>
    </div>
  </div>
</div>